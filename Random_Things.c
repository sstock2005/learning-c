// Random bits of code that are interesting to me

int main()
{
    // Create pointer to new array of size 5
    // I thought it was an interesting way to allocate memory
    int n = 5;
    char *pvowels = (char *) malloc(n * sizeof(char));
}